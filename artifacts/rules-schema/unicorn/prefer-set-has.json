[
  {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "minimumItems": {
        "type": "integer",
        "minimum": 0,
        "description": "The minimum known array size before `Set#has()` is enforced."
      }
    }
  }
]
